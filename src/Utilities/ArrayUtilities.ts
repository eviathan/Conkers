// // eslint-disable-next-line import/prefer-default-export
// export const groupBy = <TItem>(
//   array: Array<TItem>,
//   getKey: (item: TItem) => string | undefined
// ) => {
//   return array.reduce((previousValue: Test, currentValue: TItem) => {
//     const key = getKey(currentValue) ?? '';
//     // return [key].push(currentValue);
//   }, {} as Test);
// };

// type Test = { [key: string]: Array<TItem> };

// export type KeyValuePair<TKey, TValue> = {
//   key: TKey;
//   value: TValue;
// };
